name: cross-stack context ABI drift proof

on:
  pull_request:
    paths:
      - "proofs/cross-stack-context-abi/**"
      - ".github/workflows/cross-stack-context-abi.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prove:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: "22"
      - run: node proofs/cross-stack-context-abi/check.mjs
